//En todas las variables debes reemplazar el null por lo que corresponda

//Debes deifinir un string
const nuevaString = "Hola";
//Debes definir un booleano
const nuevoBool = true;
//Debes definir un número
const nuevoNum = 1;

const nuevaResta = 10 - 8 === 2;

const nuevaMultiplicacion = 2 * 2 === 4;

const nuevoModulo = 4 % 2 === 0;

module.exports = {
    nuevaString,
    nuevoBool,
    nuevoNum,
    nuevaResta,
    nuevaMultiplicacion,
    nuevoModulo
}

